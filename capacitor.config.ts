import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'capacitor-youtube-video-error-ios',
  webDir: 'dist'
};

export default config;
